<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.minor.microprocessor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".comparison"></activity>
        <activity android:name=".intro8086" />
        <activity android:name=".timingdiagram" />
        <activity android:name=".addressingmode" />
        <activity android:name=".interrupt" />
        <activity android:name=".Dashboard" />
        <activity android:name=".Books" />
        <activity android:name=".Introduction" />
        <activity android:name=".Program" />
        <activity android:name=".Topic" />
        <activity android:name=".Tutorial" />
        <activity android:name=".memories" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>